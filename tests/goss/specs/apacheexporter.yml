http:
  http://localhost:9117/metrics:
    status: 200
    body:
      - "apache_accesses_total"
service:
  apache_exporter:
    enabled: true
    running: true
process:
  apache_exporter:
    running: true
